/// Version and identity for the native engine (S0 stub; not a NegPy look claim).
public enum EngineVersion: Sendable {
    public static let protocolVersion = "0.1"
    public static let packageVersion = "0.1.0-s1"
    public static let backendName = "swift"
    public static let oracleLabel = "s1-decode"
}
