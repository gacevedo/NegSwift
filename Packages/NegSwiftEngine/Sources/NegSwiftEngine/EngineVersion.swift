/// Version and identity for the native engine (S2 log-normalize; not a full look claim).
public enum EngineVersion: Sendable {
    public static let protocolVersion = "0.1"
    public static let packageVersion = "0.1.0-s2"
    public static let backendName = "swift"
    public static let oracleLabel = "s2-log-normalize"
}
