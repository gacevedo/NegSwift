/// Version and identity for the native engine (S4a print curve; autos/Lab still off).
public enum EngineVersion: Sendable {
    public static let protocolVersion = "0.1"
    public static let packageVersion = "0.1.0-s4a"
    public static let backendName = "swift"
    public static let oracleLabel = "s4a-print"
}
